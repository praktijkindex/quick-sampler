require 'spec_helper'

describe Quick::Sampler do
  it 'has a version number' do
    expect(Quick::Sampler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
